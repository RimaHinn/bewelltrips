<h1>Add a new Retreat</h1>
      <%= simple_form_for @trip do |f| %>
        <%= f.input :name %>
        <%= f.input :address %>
        <%= f.input :description %>
        <%# <%= f.input :photos, as: :file, input_html: { multiple: true }%>
        <%= f.submit "Create my Retreat", :class => "button" %>
     <% end %>
